# Moodle Local Plugin: External CSV Import (local_extcsv)

Плагин для Moodle, позволяющий импортировать данные из внешних CSV/TSV источников, включая Google Таблицы, с настраиваемым маппингом колонок и автоматическим обновлением по расписанию.

## Возможности

- 📥 Импорт данных из CSV/TSV файлов и Google Таблиц
- 🔄 Автоматическое обновление данных по расписанию
- 🗺️ Гибкий маппинг колонок с использованием паттернов
- 📊 Предпросмотр структуры данных перед импортом
- 💾 Эффективное хранение данных в универсальных полях БД
- 🔍 API для построения SQL-запросов с логическими именами полей
- 🌐 Поддержка русской и английской локализации

## Требования

- Moodle 3.9+ (протестировано на Moodle 3.9 и 4.5)
- PHP 7.2+
- Доступ к внешним URL для загрузки данных

## Установка

1. Скопируйте папку `extcsv` в директорию `local/` вашего Moodle:
   ```
   /path/to/moodle/local/extcsv/
   ```

2. Войдите в Moodle как администратор

3. Перейдите в **Настройки сайта** → **Уведомления** (или выполните обновление через CLI)

4. После установки плагин будет доступен в разделе **Плагины** → **Локальные плагины** → **Импорт внешних CSV**

## Использование

### Добавление источника данных

1. Перейдите в **Плагины** → **Локальные плагины** → **Импорт внешних CSV** → **Управление источниками**
2. Нажмите **Добавить источник**
3. Заполните форму:
   - **Название** — имя источника
   - **Описание** — опциональное описание
   - **URL источника** — ссылка на CSV/TSV файл или Google Таблицу
   - **Тип контента** — CSV или TSV
   - **Состояние** — Включено/Отключено/Заморожено
   - **Расписание обновления** — интервал или cron выражение

### Использование Google Таблиц

Плагин автоматически преобразует URL Google Таблицы в формат экспорта. Поддерживаются:
- Прямые ссылки на экспорт: `https://docs.google.com/spreadsheets/d/{ID}/export?format=csv`
- Ссылки на редактирование: `https://docs.google.com/spreadsheets/d/{ID}/edit#gid={GID}`
- Автоматическое извлечение ID таблицы и GID листа

Пример:
```
https://docs.google.com/spreadsheets/d/1ABC123.../edit#gid=0
```

### Настройка маппинга колонок

1. После добавления источника используйте **Предпросмотр** для просмотра структуры данных
2. В конфигурации источника настройте маппинг колонок:
   ```json
   {
     "columns": [
       {
         "pattern": "Имя.*",
         "short_name": "name",
         "type": "text",
         "slot": 1
       },
       {
         "pattern": "Возраст",
         "short_name": "age",
         "type": "int",
         "slot": 1
       }
     ]
   }
   ```

### Типы данных

Поддерживаемые типы полей:
- `text` — текстовое поле (20 полей: text1-text20)
- `int` — целое число (20 полей: int1-int20)
- `float` — число с плавающей точкой (5 полей: float1-float5)
- `bool` — логическое значение (5 полей: bool1-bool5)
- `date` — дата/время (10 полей: date1-date10)
- `json` — JSON данные (3 поля: json1-json3)

### Расписание обновлений

#### Простой режим
Укажите интервал обновления:
- `30 minutes` — каждые 30 минут
- `2 hours` — каждые 2 часа
- `1 days` — раз в день

#### Продвинутый режим (Cron)
Используйте cron выражения:
- `0 2 * * *` — каждый день в 2:00
- `*/15 * * * *` — каждые 15 минут
- `0 */6 * * *` — каждые 6 часов

### Использование API для SQL-запросов

Плагин предоставляет API для построения SQL-запросов с использованием логических имён полей:

```php
use local_extcsv\source;
use local_extcsv\query_builder;

$source = new source($sourceid);
$qb = new query_builder($source);

// SQL с логическими именами полей
$sql = "SELECT {{name}}, {{age}} FROM {local_extcsv_data} WHERE {{age}} > 18";
$result = $qb->execute_query($sql);
```

## Структура базы данных

### Таблица `local_extcsv_sources`
Содержит информацию об источниках данных:
- `id` — идентификатор
- `name` — название
- `description` — описание
- `status` — состояние (enabled/disabled/frozen)
- `url` — URL источника
- `content_type` — тип контента (csv/tsv)
- `schedule` — расписание обновления
- `columns_config` — JSON конфигурация колонок
- `timecreated`, `timemodified` — временные метки
- `lastupdate`, `lastupdatestatus`, `lastupdateerror` — статус последнего обновления

### Таблица `local_extcsv_data`
Содержит импортированные данные:
- `id` — идентификатор записи
- `sourceid` — ссылка на источник
- `rownum` — номер строки в исходном CSV
- `text1`-`text20` — текстовые поля
- `int1`-`int20` — целочисленные поля
- `float1`-`float5` — поля с плавающей точкой
- `bool1`-`bool5` — логические поля
- `date1`-`date10` — поля даты
- `json1`-`json3` — JSON поля
- `timecreated` — время создания

## Права доступа

По умолчанию управление источниками доступно только администраторам сайта. Права можно настроить через:
- Capability: `local/extcsv:manage_sources`

## Автоматическое обновление

Плагин включает scheduled task, который автоматически обновляет включённые источники данных согласно их расписанию. Задача запускается каждые 15 минут через cron Moodle.

## Разработка

### Структура файлов

```
extcsv/
├── classes/
│   ├── csv_importer.php      # Импорт CSV/TSV
│   ├── data_manager.php      # Управление данными
│   ├── query_builder.php     # Построитель SQL-запросов
│   ├── source.php            # Модель источника
│   ├── source_manager.php    # Менеджер источников
│   ├── form/
│   │   └── source_form.php   # Форма редактирования
│   └── task/
│       └── update_sources.php # Задача обновления
├── db/
│   ├── access.php            # Определение capabilities
│   ├── install.xml           # Схема БД
│   └── tasks.php             # Определение задач
├── lang/
│   ├── en/
│   │   └── local_extcsv.php  # Английская локализация
│   └── ru/
│       └── local_extcsv.php  # Русская локализация
├── edit.php                  # Редактирование источника
├── index.php                 # Список источников
├── preview.php               # Предпросмотр данных
├── settings.php              # Настройки плагина
├── version.php               # Версия плагина
└── view.php                  # Просмотр данных
```

## Лицензия

GPL v3 or later

## Автор

2024

## Поддержка

При возникновении проблем или вопросов создайте issue в репозитории проекта.

